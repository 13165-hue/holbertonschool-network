#!/usr/bin/env bash
# Configures localhost to 127.0.0.2 and facebook.com to 8.8.8.8

# Use tee to overwrite the file content safely without sed -i
echo -e "127.0.0.2\tlocalhost\n8.8.8.8\tfacebook.com" | sudo tee /etc/hosts > /dev/null
